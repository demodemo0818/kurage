# i18n (英語対応) の設計と移行ガイド

2026-07-19 に方針確定。目的は**海外ユーザー獲得**で、アプリ UI・README・
Play ストア文言・Web HTML・紹介サイトまで英語化する。**全文言の英訳完了まで
言語切替は解放しない** (段階移行・一括公開)。解放リリースは **v1.1.0**。

## 仕組み

- **gen_l10n** (Flutter 公式)。設定は [l10n.yaml](../l10n.yaml)、文言は
  [lib/l10n/app_ja.arb](../lib/l10n/app_ja.arb) (テンプレート) と
  [lib/l10n/app_en.arb](../lib/l10n/app_en.arb)。
- 生成物 `lib/l10n/gen/` は **git 管理外**。`flutter pub get` /
  `flutter gen-l10n` / ビルドで自動再生成される。
- アクセスは [lib/l10n/l10n.dart](../lib/l10n/l10n.dart) 経由:
  - Widget 内: `context.l10n.foo` (拡張メソッド)
  - **BuildContext が無い層** (services / 例外メッセージ / バックグラウンド
    通知): トップレベル変数 `l10n`。MyApp.build が locale 決定のたびに
    `updateGlobalL10n` で同期する。FCM バックグラウンド isolate では
    `updateGlobalL10nFromPrefs()` で保存済み設定から初期化すること
    (widget tree が無く MyApp が走らないため)。
- 表示言語設定は `settingsProvider.appLocale` ('system' / 'ja' / 'en'、
  既定 'system')。Locale への解決は `resolveAppLocale` (非対応言語は en に
  フォールバック)。**ただし全訳完了までは main.dart が locale を ja 固定に
  しており、この設定は UI からも変更不可** (ピッカー未実装)。

## 新規文言のルール

1. ユーザーに見える文言は日本語ハードコードせず、**ja/en 両方の .arb に
   同時追加**してから `context.l10n.key` / `l10n.key` で参照する。
   CI が未翻訳キー (ja にあって en に無い) を検出して fail する。
2. キー名は lowerCamelCase。画面固有なら接頭辞 (`compose…`, `nav…`) で揃える。
3. 数値を含む文言は placeholder + 英語側は ICU plural (`relativeMinutesAgo`
   参照)。語形が分岐する文言は ICU select (`composeInProgressTitle` 参照)。
4. **debugPrint 等の内部ログ・コードコメント・docs/ は日本語のまま**でよい。
5. 英訳の用語は **Mastodon 公式 Web UI の英語表記に準拠**する
   (boost / favourite / content warning / bookmark / mute / block など。
   "favorite" ではなく英式 "favourite")。

## 移行状況 (画面単位で段階的にコミットする)

- [x] 基盤 (l10n.yaml / .arb / グローバルアクセサ / settings.appLocale / CI)
- [x] lib/main.dart (終了確認・コンポーズ破棄ダイアログ・ナビラベル・tooltip・ライセンス)
- [x] lib/utils/time_formatter.dart (相対/絶対時刻) / snackbar_helpers.dart
- [x] 小規模ページ・widget (バッチ A): app_lock_screen / app_lock_setup /
      drafts / my_profile / hashtag / edit_history / full_screen_image /
      reactors / collapsible_text / video_player / link_preview / gap_tile
- [x] 中規模 (バッチ B): poll_widget / announcements / collection_form /
      credits / column_header / dm_page / list_members / emoji_picker /
      mute_dialog / live_mode_settings_dialog / timeline_view / boss_mode
- [x] バッチ C (18〜38 文言の 16 ファイル): filters / settings /
      app_lock_settings / search / add_to_list_sheet / list_management /
      blocked_muted / collection_detail / main_page / account_settings /
      column_settings / filter_edit / scheduled_posts / profile_edit /
      thread / report
- [x] 大物・前半 (バッチ D): notifications_page (46) / app_settings (80) /
      appearance (90)
- [x] 大物・後半 (バッチ E): post_page (143) / profile_page (152) /
      post_tile (216)。post_tile のサーバー情報ダイアログ等は profile_page と
      同一文言のため `prof*` キーを共用。post_page の日時ピッカーの
      `Locale('ja')` 固定は撤去済み (アプリ locale に追従)
- [x] バッチ F (services / providers / models): mastodon_api.dart の例外
      文言 (`api*` キー、約90件) / auth_service* (`auth*`) /
      push_notification_service.dart・push_notification_style.dart
      (`push*`。`allPushChannels` は `l10n` 参照のため const → getter 化) /
      lib/models/filter.dart (`kFilterContextLabels` / `kFilterActionLabels`
      は BuildContext 引数を取る関数化) / lib/models/quote.dart /
      lib/models/conversation.dart / lib/models/json_utils.dart。
      BuildContext の無い層はグローバル `l10n` を使用 (CLAUDE.md の方針通り)。
      これで日本語ハードコードの残存は debugPrint 等の内部ログのみ
- [x] バッチ G (バッチF後の掘り起こし漏れ): app_lock_service.dart の
      local_auth `localizedReason` (`appLockBiometricReason`) /
      backup_service.dart の `FormatException` 3種 (`backup*`。
      app_settings_page.dart が `e.message` をそのまま SnackBar に
      埋め込むため、l10n 化しないと英語 UI でも日本語のまま出る経路) /
      lib/utils/app_fonts.dart のフォント選択ラベル括弧書き
      (`FontCategory` enum化 + `appearanceFontLabel` / `appearanceFontCategory*`)。
      到達不能な stub (`*_stub.dart`) の `UnsupportedError` 文言と
      `image_save_io.dart` の `XTypeGroup(label: ...)` はネイティブダイアログ
      表示用の技術的ラベルなので既存の英語直書き慣習 (`backup_file.dart` 等)
      に合わせて英語化のみ (arb 化はしない)。main.dart の OtoLogic
      ライセンスグループ名・report_page.dart の「・」区切り記号は
      対象外 (詳細は git 履歴参照)

## v1.1.0 での解放手順 (全訳完了後)

1. [x] main.dart の locale 固定を `resolveAppLocale(settings.appLocale)` ベースに
   切り替え、設定画面 (appearance_page.dart、外観設定 → 表示言語) に
   言語ピッカー (システム / 日本語 / English) を追加。
2. [x] FCM バックグラウンドハンドラ (push_notification_service.dart) の冒頭で
   `updateGlobalL10nFromPrefs()` を呼ぶ。
3. [x] README.en.md 追加 + README.md 冒頭の英語ブロックを更新
   (「英語UI未実装」注記を削除し README.en.md へリンク)。
4. [x] Play ストア英語リスティング文言を用意
   (store_assets/play_store_listing_en.md。Console 登録はユーザー作業)。
5. [x] web/auth/callback.html を `navigator.language` ベースで日英二言語化
   (web/index.html はユーザー可視文言が無いため対象外)。
6. [x] 紹介サイト demo2.jp/kurage の英語版 (site/ 側、別リポジトリ)。
   同一 URL・JS 言語トグル方式 (data-i18n 属性 + I18N 辞書)。トップページ・
   プライバシーポリシー・アカウント削除ページに適用。child-safety は
   既に英語サマリーを静的併記済みのため対象外。site/ 側でコミット済み、
   デプロイ (`site/deploy.ps1`) は別途実施が必要。
7. [ ] RELEASING.md に従い v1.1.0 としてリリース。
